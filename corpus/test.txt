========
comment
========

// This is a comment.

---
(program (comment))


========
statement
========

C(5005) <<
  ($v1*8000*4, sin(1000 / ($v1%4000)*2))

---
(program
  (statement
    (connect (keyword_identifier) (connect_arguments (port)))
    (message
      (expression
        (binary_operator (binary_operator (dollar_variable (identifier)) (number)) (number)))
      (expression
        (function (name)
          (arguments
            (binary_operator
              (binary_operator
                (number)
                (parenthesized
                  (binary_operator (dollar_variable (identifier)) (number))))
              (number))))))))
